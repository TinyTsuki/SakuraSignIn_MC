modLoader = "javafml"
loaderVersion = "${loader_version_range}"
license = "${mod_license}"
logoFile = "logo.png"

[[mods]]
    modId = "${mod_id}"
    version = "${mod_version}"
    displayName = "${mod_name}"
    updateJSONURL = "https://update.mc.vanilla.xin/${mod_id}/${loader_type}/${minecraft_version}"
    displayURL = "https://mc.vanilla.xin/${mod_id}"
    authors = "${mod_authors}"
    description = '''${mod_description}'''
    clientRequired = false

[[mixins]]
    config = "${mod_id}.mixins.json"

[[dependencies."${mod_id}"]]
    modId = "neoforge"
    type = "required"
    versionRange = "${neo_version_range}"
    ordering = "NONE"
    side = "BOTH"

[[dependencies."${mod_id}"]]
    modId = "minecraft"
    type = "required"
    versionRange = "${minecraft_version_range}"
    ordering = "NONE"
    side = "BOTH"

[[dependencies."${mod_id}"]]
    modId = "banira_codex"
    type = "required"
    versionRange = "${banira_version_range}"
    ordering = "AFTER"
    side = "BOTH"
